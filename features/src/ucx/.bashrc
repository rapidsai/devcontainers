export UCX_VERSION="${UCX_VERSION}";
if test -n "${OMPI_MCA_btl}"; then export OMPI_MCA_btl="${OMPI_MCA_btl}"; fi
if test -n "${OMPI_MCA_pml}"; then export OMPI_MCA_pml="${OMPI_MCA_pml}"; fi
if test -n "${OMPI_MCA_osc}"; then export OMPI_MCA_osc="${OMPI_MCA_osc}"; fi
if test -n "${OPENMPI_VERSION}"; then export OPENMPI_VERSION="${OPENMPI_VERSION}"; fi
